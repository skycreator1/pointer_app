import 'package:flutter/material.dart';

final class AppColorsLight {
  const AppColorsLight._();

  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF5F5F5);
  static const primary = Color(0xFF1A1A1A);
  static const accent = Color(0xFFFF4444);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF8A8A8A);
  static const border = Color(0xFFE0E0E0);
  static const online = Color(0xFF34C759);
  static const offline = Color(0xFFC7C7CC);
}

final class AppColorsDark {
  const AppColorsDark._();

  static const background = Color(0xFF000000);
  static const surface = Color(0xFF1C1C1E);
  static const primary = Color(0xFFFFFFFF);
  static const accent = Color(0xFFFF453A);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8E8E93);
  static const border = Color(0xFF38383A);
  static const online = Color(0xFF30D158);
  static const offline = Color(0xFF48484A);
}

