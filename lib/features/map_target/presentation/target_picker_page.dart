import 'package:flutter/material.dart';

class TargetPickerPage extends StatelessWidget {
  const TargetPickerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Target picker placeholder')),
    );
  }
}
