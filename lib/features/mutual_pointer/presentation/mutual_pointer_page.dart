import 'package:flutter/material.dart';

class MutualPointerPage extends StatelessWidget {
  const MutualPointerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Mutual pointer page placeholder')),
    );
  }
}
